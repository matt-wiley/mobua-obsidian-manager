# 2026-07-07 Pass 01 — Init
_relates: [[index]]_

## What Changed
- Bootstrapped wiki at `./docs/llm-wiki/`. First run — no prior wiki existed.
- Wrote `meta/schema.md` with 4 content types: decision, pattern, concept, component (component added at user request beyond default 3).
- Created 18 content pages:
  - decisions (2): markdown-source-of-truth, atomic-md-writes
  - patterns (5): all-sql-in-queries, all-fetch-in-lib-api, one-component-per-field-type, frontmatter-type-inference, stable-uuid-and-hash-skip
  - concepts (6): emergent-schema, wikilink-navigation, relation-field-resolution, sync-badge-states, atomic-write-delete-debounce, backend-package-layout
  - components (5): parser, indexer, writer, watcher, sse-event-flow
- Wrote INDEX.md (inline sections, no sub-indexes — all types under 8-entry threshold), wiki-health.md.
- Verified `relates:`↔inline mirror consistency; fixed 5 pages during audit.

## Why
Captured the non-obvious spine of the system that code alone doesn't state: why markdown is canonical, the atomic-write ↔ delete-debounce coupling, path-derived identity, the emergent (query-time) schema, and the field-type inference/routing chain. Also recorded a real gotcha — the live package is `backend/obsidian_manager/`, and both the top-level `backend/db|api|sync` tree and CLAUDE.md's unprefixed paths are stale.

## Gaps Remaining
- Frontend stores, HTTP API surface, table view + quick-search, and `col_widths` persistence undocumented.
- Stale duplicate backend tree not removed (out of scope for a wiki-init pass).
